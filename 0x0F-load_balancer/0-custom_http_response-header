#!/usr/bin/env bash
# sets up server with a redirect and custom 404
apt-get update -y
apt-get install Nginx -y
MYHOST=$(cat /etc/hostname)
n5="add_header X-Served-By $MYHOST;"
sudo sed -i "29i $n5" /etc/nginx/sites-available/default
service nginx restart
